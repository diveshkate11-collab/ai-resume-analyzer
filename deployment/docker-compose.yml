services:
   ai-resume-copilot:
      build:
         context: ..
         dockerfile: Dockerfile

      container_name: ai-resume-copilot

      ports:
        - "8000:8000"

      volumes:
        - ../:/app
    
      restart: unless-stopped